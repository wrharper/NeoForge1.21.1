{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Client",
            "type": "shell",
            "command": "./gradlew runClient",
            "group": { "kind": "build", "isDefault": true }
        }
    ]
}

